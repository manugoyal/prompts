prompt: 'Perform the following coding operation: $op. Just emit the code. No extra text,
    just the code. No delimiting backticks, just the code. You may use the following input information:
    ```
    $input
    ```'
